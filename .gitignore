node_modules/
dist/

# runtime data
data/

# secrets - none are meant to be committed; all settings (drive UUID,
# mount point, folder names, auto-heal tuning) are entered in the
# dashboard or via DRP_* first-boot env seeds, never baked into a
# tracked file.
.env

*.tsbuildinfo
